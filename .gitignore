# pnpm content-addressable store (local cache, never commit)
.pnpm-store/

# Playwright MCP runtime output
.playwright-mcp/

# Write-permission test artifacts
.write_test_*

# Disabled stale nested git repo (was 5-Kod/.git; renamed, kept local-only)
5-Kod/.git.disabled-stale/

# Large binary design-underlag — kept local, out of git (the extracted design
# system already lives committed in 5-Kod). Relocated out of the repo root.
4-Dokument-Underlag/06-skarmdumpar-bygg/
2-Byggplan/Corevo Booking Design System-handoff/
# Same handoff copies under their relocated paths (path-anchored rule above misses
# these): the completed-goals folder picked up a copy, and a "v2" export landed in
# 2-Byggplan. ~13 MB of PNGs/HTML prototypes — extracted essentials already in 5-Kod.
2-Byggplan/_klart/Corevo Booking Design System-handoff/
2-Byggplan/corevo-booking-design-system v2/
# v3 = the "maxed" handoff (goal-17 Steg 0 source-of-truth). 23.6 MB of
# screenshots/prototype HTML — kept on disk as the contract, ignored like v2.
# Canon tokens verified identical to the playbook (zero token delta).
2-Byggplan/corevo-booking-design-system v3/

# Stray vite/vitest cache that materializes as a node_modules dir at the repo root
# (no real install lives here — the workspace install is under 5-Kod). Standard.
node_modules/

# ce-optimize restoran harness — local measurement artifacts (never committed):
# the merged metrics snapshot + the dual fidelity screenshots.
5-Kod/apps/web/lib/sajtbyggare/_optimize/.last-metrics.json
4-Dokument-Underlag/skarmdumpar-bygg/restoran-ours.png
4-Dokument-Underlag/skarmdumpar-bygg/restoran-vendor.png

# Scratch / editor / build junk
*.vsix
*.log
# goal-32: generated PROD deploy config (wrangler.jsonc routes ∪ DB customer
# domains). Build artifact — produced by scripts/gen-deploy-config.mjs at deploy
# time, never hand-edited, never committed. Source of truth = wrangler.jsonc.
5-Kod/apps/web/wrangler.deploy.json
2-Byggplan/.design-canvas.state.json
4-Dokument-Underlag/01-acceptans/.design-canvas.state.json
# temp goal-17 render-verify capture harness (deleted post-verify; ignore if regenerated)
5-Kod/_goal17-cap.mjs

# Template-katalog: rådumpen (100+ templates) hålls utanför git — bara kandidater/valda versioneras
4-Dokument-Underlag/03-template-katalog/00-inbox/
# Råa vendor-dumpar (73 MB) landade i katalog-roten istället för 00-inbox/ — håll dem
# lokala likt resten av rådumpen. Bara KATALOG.md + 01-kandidater/02-valda versioneras.
4-Dokument-Underlag/03-template-katalog/alotan-master/
4-Dokument-Underlag/03-template-katalog/BarberX-master/
4-Dokument-Underlag/03-template-katalog/barberz-master/
4-Dokument-Underlag/03-template-katalog/base-1.0.0/
4-Dokument-Underlag/03-template-katalog/brber-master/
4-Dokument-Underlag/03-template-katalog/carserv-1.0.0/
4-Dokument-Underlag/03-template-katalog/Free.Bundle.2023/
4-Dokument-Underlag/03-template-katalog/haircare-master/
4-Dokument-Underlag/03-template-katalog/hairsal-gh-pages/
4-Dokument-Underlag/03-template-katalog/materio-1.0.0/
4-Dokument-Underlag/03-template-katalog/razor-master/

# Råa template-data-dumpar (fill/backfill) — körs mot DB out-of-band, ej i git.
# templates-import.sql versioneras dock (kanonisk import, se nedan).
4-Dokument-Underlag/03-template-katalog/*-fill.sql
4-Dokument-Underlag/03-template-katalog/*-fill-2.sql
4-Dokument-Underlag/03-template-katalog/*-backfill.sql

# Claude Cowork session-artefakter
.claude/
.superpowers/
mappar i katalog-roten (numrerade 01–100
# + namngivna) — licens-/storleksskäl. Ignorera bara KATALOGER; rot-filer (KATALOG.md,
# KATALOG-RAPPORT.md, templates-staging.json, templates-import.sql, _*-script) versioneras.
4-Dokument-Underlag/03-template-katalog/*/
!4-Dokument-Underlag/03-template-katalog/01-kandidater/
!4-Dokument-Underlag/03-template-katalog/02-valda/
5-Kod/supabase/.temp/

# uiverse-dumpen = lokal verktygslada (Zivars klipp), inte repo-innehall
4-Dokument-Underlag/uiverse-komponentbibliotek.md

# goal-61 verify-maskinens lokala matskript (dev-only, refereras fran memory)
5-Kod/apps/web/_*.mjs
5-Kod/apps/web/_sweep/
5-Kod/apps/web/_geom-*.json
5-Kod/docs/ops/INLOGGNINGAR.md

# Isolerade Codex-arbetsytor — får aldrig versionshanteras som projektinnehåll.
.worktrees/
