<!-- VÅG 2 syntes — låsta dirigent-beslut ur recon-flottan (wt92bllyb, 4 agenter). Driver bygget. -->
# VÅG 2 — Syntes + låsta beslut (migrations-rekoncil + lojalitet + bokningsspårbarhet)

> Recon-DDL/kod-detalj: `tool-results/betlvr47d.txt` + task-output `wt92bllyb`. Detta = besluten + ordningen.

## Migrationsnumrering (strict append, ALLA > dormant 0014)
0014_slot_holds = **DORMANT, EJ applicerad** (Zivar-OK-gated header; orthogonal mot pending-squat per r4 — wizard-fas, inte redan-insatt pending → löser INTE squaten). Stabil oapplicerad (överlevt flera deploys). Mina nya, applicerade i ordning via MCP:
| Nr | Fil | Innehåll | Beror på |
|---|---|---|---|
| **0015** | `booking_customer_id_resolve.sql` | `private.resolve_customer_id()` + ersätt `create_public_booking` med 9-arg (additivt `customer_id`, profile_id ORÖRT) | — |
| **0016** | `loyalty_earn.sql` | AFTER UPDATE-trigger på bookings (→completed) → 1 `loyalty_ledger`-rad (earn_completed, idempotent) | 0015 (customer_id) |
| **0017** | `booking_traceability_softdelete.sql` | `booking_status_history` + audit-trigger + hard-delete-guard + tenants soft-delete + CASCADE→RESTRICT-decouple | — |
| **0018** | `pending_expiry_sweep.sql` | `expire_abandoned_pending_bookings(ttl)` RPC, service_role-only | 0017 (loggar till history) |
| 0019 | (VÅG 3) | `resolve_tenant_by_domain` — goal-16 re-pekas hit (ej "0011") | — |

Apply-ordning: 0015→0016→0017→0018. Varje: `set search_path=''`, schema-kvalad, SECURITY DEFINER där RLS kräver, idempotent, transaktion + AFTER-verify + advisor (baslinje **15 WARN/0 ERROR** får ej regressa) + regen `packages/db/types.ts`.

## Låsta beslut
1. **customer_id = ADDITIVT** (r2). `create_public_booking` (9-arg: + p_guest_name/email/phone) skriver `customer_id` via `private.resolve_customer_id` (authed→nyckel `(tenant,auth_user_id)`, contact_hash NULL; gäst→`(tenant,contact_hash)`; ingen hash→NULL). **`customer_profile_id` repekas ALDRIG.** Authed-gren sätter ALDRIG contact_hash (kollisions-fälla). Walk-in (`createWalkIn`) fångar ej kontakt → customer_id NULL, **dokumenterad follow-up** (ej blockerande). DROP+recreate RPC i transaktion + re-grant (mirror 0009). Types-regen.
2. **GDPR co-dependency (MANDATORY, blockerar 0015):** `erase.ts` MÅSTE utökas att anonymisera matchande `public.customers`-rad (`status='anonymized'` → fyrar M5-scrub-trigger + nulla full_name/email/phone/contact_hash). Annars skriver varje ny gäst-bokning o-raderbar PII. Stänger även plan-beslut #10 (0011 P2). Samma release.
3. **/konto gäst-synlighet = DEFERRED** (separat merge-steg contact_hash→auth_user_id ELLER /konto läser även customer_id). Ej i denna fix.
4. **Lojalitet earn (r1):** AFTER UPDATE WHEN →'completed', SECURITY DEFINER, idempotent via `loyalty_ledger_earn_once` (ON CONFLICT (booking_id) WHERE reason='earn_completed' DO NOTHING). **Failure-isolering:** `EXCEPTION WHEN OTHERS THEN RETURN NEW` — en lojalitets-miss får ALDRIG abborta status-UPDATE (speglar review-nudge-kontraktet). customer_id resolve-or-skip (NULL→RETURN NEW, ingen FK-violation). tenant_id ur `NEW.tenant_id` (ALDRIG `private.tenant_id()` i trigger). Earn-rate = `tenant_settings.settings.loyalty.points_per_visit`, **default 50** (per-visit fast, ej 1pt/SEK). REDEEM = DEFERRED. Endast AFTER UPDATE (walk-in insertar 'confirmed', ej 'completed').
5. **Spårbarhet (r3):** `booking_status_history` (booking_id FK CASCADE, **tenant_id NO-FK** så historik överlever, from/to-status, changed_by, source, rebooked_from/to). AFTER INSERT OR UPDATE-trigger `record_booking_status_change` (SECURITY DEFINER — anon gäst saknar tenant-claim). **bsh = mandatory/in-transaction** (garanterad historik); **audit_log-mirror = best-effort** (EXCEPTION-subblock, bevarar "audit blockerar aldrig"-invarianten). Hög-fail loud ≠ tyst försvinnande. Status-oförändrad UPDATE → ingen rad (reschedule/GDPR-scrub tyst). Actor/reason via `set_config`/`current_setting`. source = coarse (auth.uid null-ness) v1.
6. **Hard-delete-guard:** `block_booking_hard_delete` BEFORE DELETE på bookings (raise). GDPR-säkert (erase UPDATEar, raderar ej). 2 test-fixtures som DELETEar → gate via tx-local GUC `corevo.allow_booking_delete` satt i test-setup.
7. **Tenants soft-delete:** `status` CHECK in ('active','suspended','deleted'). `setTenantStatus` accepterar 'deleted' (flip, ej delete). **Behåll** createTenant-rollback hard-`delete()` (historiklös create-abort). `audit_log.tenant_id` + `bookings.tenant_id` CASCADE→**RESTRICT** (NOT NULL → SET NULL omöjligt; RESTRICT + soft-delete = historik överlever). Barn-tabeller (tenant_settings/locations/roles) behåller CASCADE (rollback städar). `getAdminTenant` läser status, returnerar null på 'deleted' (annars muterar soft-deletad tenants admin). suspended = oförändrat (storefront mörkar redan).
8. **Pending-expiry (r4):** DEFER 0014. `expire_abandoned_pending_bookings(p_ttl_min default 30)` — cancel 'pending' bokning ENDAST om den har en `payments`-rad status='pending' äldre än TTL (toggle-säker diskriminator: payments-rad finns iff online-checkout körde; non-paying terminal-pending = orörd). **service_role-only grant.** Reuse status='cancelled' (ingen ny enum). Cron: Cloudflare cron-route (konsistent med befintlig reminders-cron) → service_role-RPC. Inert idag (0 payment-rader live).
9. **Refund-paritet (r4):** lägg `refundBookingPayment` i (a) `cancelByToken`, (b) admin `setBookingStatus`→cancelled, (c) webhook-gren när succeeded-payment landar på redan-cancelled bokning (+ skippa kvitto då). Alla idempotenta (refund.ts no-op om ej 'succeeded'/redan 'refunded'). Inert idag.
10. **Admin status-guard (r4):** `ALLOWED_FROM`-matris på admin `setBookingStatus` (cancelled/no_show i INGEN from-set → kan ej tyst återupplivas; 0 rader = blockerad). Review-nudge under `if(updated)`. Revive-after-cancel = via re-book, ej status-flip.

## Bygg-ordning
DB (SOLO, sekventiellt, apply+verify per migr): 0015 → 0016 → 0017 → 0018. Sen app-kod: `boka/actions.ts` (guest-params), `avboka/actions.ts` (refund), `admin/actions.ts` (ALLOWED_FROM + refund + nudge-gate), `api/stripe/webhook/route.ts` (2b-gren), `platform/actions.ts` (setTenantStatus 'deleted' + audit), `admin/tenant.ts` (status-guard), `gdpr/erase.ts` (customers-anonymisering), cron-route (pending-expiry). Types-regen. Gates. Adversariell verifiering (Workflow). Deploy. Commit + push.
