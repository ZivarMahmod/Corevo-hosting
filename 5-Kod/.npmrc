auto-install-peers=true
strict-peer-dependencies=false
# Flat node_modules so transitive ESLint plugins (eslint-config-next chain)
# resolve from any workspace package. Also maximizes Next/OpenNext compat.
node-linker=hoisted
