// @corevo/ui — shared design tokens + theming helpers (ADR 01 §3).
// The component engine is identical for every tenant; only tokens/variants change.
export { injectTenantTokens, type TenantBranding } from './tokens'
